spring:
  application:
    name: identity-service
  datasource:
    url: jdbc:mysql://localhost:3636/identity-db
    username: root
    password: Hoang@2002
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format-sql: true
server:
  port: 8081